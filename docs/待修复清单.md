# 待修复清单（技术债 / 已知问题）

> 2026-06-10 多功能重构 code review 时记录。均为**已知、暂不修**的项——不影响当前使用，
> 但以后要还。修掉一项就把它划掉并注明日期。

## P1 · 影响体验，找时间修

1. ~~**删除会话不清理图片文件**~~ ✅ 2026-06-10 已修：`deleteSession` 删行前按消息
   `imageUri` 逐个删除本地 JPEG。

2. ~~**录音器全局单例，跨页可冲突**~~ ✅ 2026-06-10 已修：`PcmAudioRecorder.start()`
   发现上一段录音未停时先 `stop()` 丢弃，保证拿到干净的新录音。

3. ~~**图片只在发送当轮喂给模型，追问不"重看"原图**~~ ✅ 2026-06-10 已修（用户真机反馈）：
   无新附图时回看上下文窗口内最近一张已发图片、每轮重新 `includeImage`；带图轮次在
   历史里标注"（发送了一张图片）"；图片被压缩进摘要后自然停止重喂（控制耗时代价）。
   取舍：图在窗口内时每轮多一次视觉前缀（变慢些）。

## P2 · 正确性边角，低频触发

4. ~~**上下文压缩摘要的承上启下文案中文写死**~~ ✅ 2026-06-11 已修：
   chatSystem/图片标注/摘要桥接全部收口 PromptTemplates 并按应用 Locale 中英切换。

5. ~~**问答 `stop()`（暂停保留部分回答）不更新会话 `updatedAt`**~~ ✅ 2026-06-10 已修：
   stop() 落库后调用新加的 `ChatDao.touchSession`。

6. ~~**翻译历史不去重**~~ ✅ 2026-06-10 已修：插入前 `deleteDuplicates`（同原文+方向只留最新）。

7. ~~**底栏首帧闪烁**~~ ✅ 2026-06-10 已修：`currentRoute` 为 null 时视作 Translate 路由。

## P3 · 工程债，产品验证后再还

8. **没有单元测试基建**
   无 `app/src/test`、无 JUnit。纯逻辑其实可测：压缩窗口算术（摘要+takeLast 不超 8）、
   prompt 拼装、`pcmToWav`/`peakPcm`、`MIGRATION_*`（Room 迁移测试）。
   修法：先搭 JUnit + Room migration test，从纯函数开始补。

9. **ViewModel 偏厚，缺 Repository/UseCase 层**
   `ChatViewModel`（会话+录音+图片+压缩）和 `TranslateViewModel`（翻译+ASR+历史）职责过多。
   修法：抽 `ChatRepository` / `TranslateRepository`，VM 只留 UI 状态编排。

10. **vision/voice 模块仍有 `pcmToWav`/图片解码私有副本**
    公共版已抽到 `engine/audio/WavUtils.kt`、`engine/image/ImageUtils.kt`，但未挂载的
    vision/voice 模块内部副本未替换。这两个模块重新挂载 UI 时统一改用公共版。
    （2026-06-10：ChatViewModel 的私有 pcmToWav 副本已删，改用公共版。）

11. ~~**`PlaceholderScreen` 已无引用（dead code）**~~ ✅ 2026-06-10 已删。

## CI

13. **release.yml 里的 actions 仍跑 Node 20，GitHub 2026-06-16 起强制 Node 24**
    v1.0.5 发版日志的弃用警告：`actions/checkout@v4`、`actions/setup-java@v4`、
    `android-actions/setup-android@v3`、`gradle/actions/setup-gradle@v4`。
    下次发版（v1.0.6）**前**把它们升到支持 Node 24 的大版本（checkout@v5 等），
    否则 6 月 16 日后 tag 触发的发版可能直接挂。
    （发布步已于 2026-06-10 改为 gh CLI，不在此列。）

## 跨端

12. ~~**iOS 端待真机验收 + 功能对齐**~~ ✅ 2026-06-11 完成：iOS 从零起盘到四 Tab
    全量对齐（翻译+语音/问答会话压缩图片语音/历史/设置含模型源与语言/启动页/图标），
    用户真机验收通过；模型下载（ModelScope+后台会话+断点续传）与更新链路同步对齐。
    剩余差异仅平台特性：iOS 无应用内自更新（App Store 承担）。

## 产品已知取舍（非 bug，记录在案）

- 上下文压缩为 ≤200 字摘要，**细节有损**（确切数字/措辞可能丢）——离线小模型长会话的代价。
- （2026-06-10 重设计）上下文=摘要 + **全部未压缩双方消息**（用户+AI），从 DB 直读；
  压缩触发=未压缩 >20 条 **或** 总字数 >3000（会话"满了"），压缩后保留最近 6 条原文；
  压缩没跟上时按 3000 字预算从最旧的开始裁剪兜底。回答完成后后台静默执行；失败静默、下轮重试。
- 阈值常量收口在 `ChatViewModel` companion（`COMPRESS_AFTER_MESSAGES=20` /
  `CONTEXT_CHAR_BUDGET=3000` / `KEEP_RAW_AFTER_COMPRESS=6`），要调优改这里。
