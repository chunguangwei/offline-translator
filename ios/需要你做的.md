# 译人 iOS V1（翻译先行）— 真机验收步骤

> 2026-06-11。模拟器上 UI 冒烟已通过（Claude 本机 Xcode 26.5 自验）。
> 真机需要你的 Apple ID 签名（免费账号即可，7 天有效期，过期重装）。

## 一次性准备

1. 打开工程：
   ```bash
   cd "ios" && xcodegen generate && open Yiren.xcodeproj
   ```
   （改过 `project.yml` 或拉新代码后重跑 `xcodegen generate` 即可，工程文件不入库。）
2. Xcode 里选中 Yiren target → **Signing & Capabilities**：
   - Team 选你的 **Personal Team**（登录 Apple ID 即出现）
   - Bundle Identifier 若提示被占用，改成带你名字的，如 `com.weichunguang.yiren`
3. iPhone 用线连 Mac，首次需在手机上「信任此电脑」+
   设置 → 隐私与安全性 → 开发者模式 → 打开。

## 验收流程（2026-06-11 起：模型已随包，装上即用）

> ImagePilot 同款"模型随包"：`ios/BundledModels/` 里的 E2B 模型会一起装进手机
>（包约 2.5GB，USB 传输 1–3 分钟），**无需在手机上下载任何东西**。

1. Xcode 顶部设备选你的 iPhone → **⌘R** 运行（首次安装传 2.5GB 包，耐心 1–3 分钟）。
2. 打开 app → 翻译页**不应**出现"还没有可用的模型"横幅（模型已内置）。
3. 输入「你好，世界」→ 点「翻译」：
   - 首次会显示「正在加载模型，请稍等…」（真机预计 10–60 秒）
   - 随后译文**流式**出现（模拟器实测输出 "Hello, world"）
4. 点「停止」能中断；上下互换按钮能换方向（English→中文也试一句）。
5. （可选）设置 Tab → 模型管理：E2B 应显示「已内置 · 随包可用」。

> **iPhone 13（4GB 内存）专项**：工程已加 `increased-memory-limit` entitlement
> 提升内存上限。如果加载模型时 app 仍闪退/被杀，把现象告诉 Claude。

## 已知约束（V1）

- 免费账号签名 **7 天过期**，过期后 app 打不开，重新 ⌘R 装一次即可（模型不丢）。
- V1 仅文本翻译；语音 / 图片问答 / 会话 / 历史 / 启动页在后续阶段对齐 Android。
- 建议 6GB RAM 设备（iPhone 13 Pro / 14 Pro 及之后）；不足可能加载失败或被系统杀。

## 有问题贴给 Claude 的信息

- Xcode 控制台红字（尤其 `LiteRTLM` / `dyld` / 内存相关）
- 翻译页错误条的文字
- 机型 + iOS 版本
